/*using Godot;
using System;

public partial class FOWController : Control
{

	[Export] FogOfWarElement fogElement;
    [Export] FogOfWarElement shroudElement;

    public void InitFOW(Vector2 topLeft, Vector2 mapSize)
    {
        fogElement.InitFOW(topLeft, mapSize);
        shroudElement.InitFOW(topLeft, mapSize);
    }

    public void AddSightComponent(FOWSightComponent component)
    {
        fogElement.AddSightComponent(component);
        shroudElement.AddSightComponent(component);
    }

    public void AddBlockerComponent(FOWBlockerComponent component)
    {
        fogElement.AddBlockerComponent(component);
        shroudElement.AddBlockerComponent(component);
    }

    public void RemoveSightComponent(FOWSightComponent component)
    {
        fogElement.RemoveSightComponent(component);
        shroudElement.RemoveSightComponent(component);
    }

    public void RemoveBlockerComponent(FOWBlockerComponent component)
    {
        fogElement.RemoveBlockerComponent(component);
        shroudElement.RemoveBlockerComponent(component);
    }
}
*/